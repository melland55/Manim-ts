/**
 * moving_camera — barrel export for the moving_camera module.
 *
 * TypeScript port of manim.camera.moving_camera.
 */

export {
  MovingCamera,
  CameraFrame,
  CameraFrameAnimateProxy,
} from "./moving_camera.js";

export type {
  IMovableFrame,
  MovingCameraOptions,
} from "./moving_camera.js";
