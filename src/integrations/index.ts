// Entry points are accessed via subpath imports:
//   "manim-ts/react" → ./react
//   "manim-ts/vue"   → ./vue
// This barrel is intentionally empty to avoid pulling react/vue as deps
// of the main package.
export {};
