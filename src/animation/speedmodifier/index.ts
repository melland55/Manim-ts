/**
 * animation/speedmodifier — manim-ts
 *
 * Utilities for modifying the speed at which animations are played.
 * Python: manim/animation/speedmodifier.py
 */

export { ChangeSpeed } from "./speedmodifier.js";
export type {
  IAnimationExt,
  IAnimationGroup,
  IWait,
  ChangeSpeedOptions,
} from "./speedmodifier.js";
